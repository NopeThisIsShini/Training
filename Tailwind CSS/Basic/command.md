```
Responsive Design
 ```
 ```
 Button
 Hover (after taking the coursor to the button)
 After (after clicking the button)
 focus (after pressing tab when this particular button is selected), better to avoid use active and focus at a time

 class="active:bg-purple-900 focus:bg-black"
 ```